# Tag Manager

Organize projects with tags.

## Usage

```bash
python3 cli.py add "mcp" "agent-memory"
python3 cli.py list
python3 cli.py find "mcp"
```

## Example

```
📁 1 tags:

  mcp (2):
    - agent-memory
    - mcp-discovery
```